@page
@model FoodNutritionTracker.Pages.IndexModel
@{
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Nutrition Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #00A9FF, #89CFF3); /* Vibrant gradient */
            color: #fff;
            text-align: center;
            padding: 50px;
            font-family: 'Roboto', sans-serif;
        }

        .container {
            max-width: 600px;
            margin: auto;
            background: #ffffff;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #34495E; /* Deep, professional blue */
            font-weight: bold;
            font-size: 2.5rem;
            margin-bottom: 30px;
        }

        .btn-custom {
            width: 100%;
            margin-top: 15px;
            padding: 15px;
            font-size: 18px;
            border-radius: 8px;
            transition: all 0.3s;
            font-weight: bold;
        }

            .btn-custom:hover {
                transform: scale(1.05);
            }

        .btn-primary {
            background-color: #F39C12; /* Vibrant yellow-orange */
            border-color: #F39C12;
        }

            .btn-primary:hover {
                background-color: #E67E22;
                border-color: #E67E22;
            }

        .btn-success {
            background-color: #2ECC71; /* Fresh green */
            border-color: #2ECC71;
        }

            .btn-success:hover {
                background-color: #27AE60;
                border-color: #27AE60;
            }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Food Nutrition Tracker</h1>
        <button class="btn btn-primary btn-custom" onclick="window.location.href='/Scanner/Index'">📷 Barcode Scanner</button>
        <button class="btn btn-success btn-custom" onclick="window.location.href='/MealSuggestions/MealSuggestions'">🥗 Meal Suggestions</button>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
